{
  "name": "@heliosproto/web",
  "version": "0.0.0",
  "private": true,
  "description": "Helios Protocol web pillar — wallet app, browser extension, React SDK",
  "license": "Apache-2.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "biome check .",
    "format": "biome format --write .",
    "dev": "pnpm --filter @heliosproto/app dev"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.0.0",
    "typescript": "^5.6.0"
  },
  "packageManager": "pnpm@10.15.1",
  "engines": {
    "node": ">=20"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild", "sharp", "@biomejs/biome", "unrs-resolver"]
  }
}
